[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "bonus-bot"
version = "0.1.0"
requires-python = ">=3.11,<3.12"
dependencies = [
    "aiogram==2.25.1"
]
